.site-footer-wrapper {
  background: #455A64;
  margin-bottom: 50px;
  .site-footer {
    padding: 30px 0;
    margin: 0 auto;
    font-size: 15px;
    max-width: 1024px;
    .site-footer-link {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: center; 
      .site-footer-col {
        display: flex;
        flex-direction: column;
        padding-bottom: 30px;
        // width: 33.33333%; 
        padding-right: 30px;
      }
    }
  }
}